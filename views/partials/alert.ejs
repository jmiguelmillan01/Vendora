<% if (typeof flash !== 'undefined' && flash) { %>
  <div
    class="mb-4 rounded-lg border px-4 py-3 text-sm <%= flash.type === 'error' ? 'bg-red-50 border-red-200 text-red-700' : 'bg-green-50 border-green-200 text-green-700' %>"
    role="alert"
  >
    <%= flash.message %>
  </div>
<% } %>
